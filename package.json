{
  "name": "ksm-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && npm run check:content",
    "preview": "wrangler dev",
    "check:content": "node scripts/check-content.mjs dist",
    "deploy": "astro build && npm run check:content && wrangler deploy",
    "db:init": "wrangler d1 execute ksm-leads --remote --file ./schema.sql",
    "db:init:local": "wrangler d1 execute ksm-leads --local --file ./schema.sql"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.2.6"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.18",
    "tailwindcss": "^4.1.18",
    "wrangler": "^4.125.0"
  }
}
